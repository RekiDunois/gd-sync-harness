# SQLite state database (local control plane)
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite.bak
*.sqlite.bak-*

# Local state/data directories (root-level only, not internal/state source)
/.local/
/state/
/data/

# OAuth credentials and rclone config (never commit secrets)
rclone.conf

# Build artifacts
/bin/
*.exe
*.dylib
*.test
coverage.out

# macOS
.DS_Store

# Logs
*.log
logs/
