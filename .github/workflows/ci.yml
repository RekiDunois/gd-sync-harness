name: CI

on:
  pull_request:
  push:
    branches:
      - master

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: verify (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install rclone (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y rclone

      - name: Install rclone (macOS)
        if: runner.os == 'macOS'
        run: brew install rclone

      - name: Show tool versions
        run: |
          go version
          rclone version

      - name: Verify dependencies
        run: go mod verify

      - name: Vet
        run: go vet ./...

      - name: Build CLI
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          go build -trimpath -o dist/knowledge-sync ./cmd/knowledge-sync
          test -x dist/knowledge-sync
          go version -m dist/knowledge-sync

      - name: Smoke-test built CLI
        shell: bash
        run: |
          set -euo pipefail
          ./dist/knowledge-sync --version
          ./dist/knowledge-sync --help >/dev/null
          ./dist/knowledge-sync profile --help >/dev/null
          ./dist/knowledge-sync profile prune --help >/dev/null

      - name: Test full suite
        run: go test -count=1 ./...

      - name: Verify tests leave checkout clean
        shell: bash
        run: |
          set -euo pipefail
          git diff --exit-code
          test -z "$(git status --porcelain --untracked-files=all)"
